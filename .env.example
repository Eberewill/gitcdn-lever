# APP_URL: Public URL where the app is running.
# Local example: http://localhost:3000
APP_URL="http://localhost:3000"

# GITHUB_CLIENT_ID / GITHUB_CLIENT_SECRET:
# Values from your GitHub OAuth App.
# OAuth callback URL should be:
#   http://localhost:3000/api/auth/callback (local)
#   https://<your-vercel-domain>/api/auth/callback (production)
GITHUB_CLIENT_ID=""
GITHUB_CLIENT_SECRET=""

# SESSION_SECRET:
# Random secret used to sign session cookies.
# Example: openssl rand -hex 32
SESSION_SECRET=""

# TOKEN_ENCRYPTION_KEY:
# Secret used to encrypt GitHub access tokens at rest.
# Example: openssl rand -hex 32
TOKEN_ENCRYPTION_KEY=""
